import React from 'react'
import "./Pagination.css"

const Pagination = () => {
    return (
        <section className="pagination">
            Pagination
        </section>
    )
}

export default Pagination
